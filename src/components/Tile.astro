---
const bgColors = [
    "hover:bg-red-700",
    "hover:bg-blue-700",
    "hover:bg-green-700",
    "hover:bg-yellow-700",
];

const randomColor = bgColors[Math.floor(Math.random() * bgColors.length)];
---

<div class:list={"outline outline-white h-full w-full " + randomColor}></div>
